@echo off 
::this will make it so the commands don't show when they're running.
:: Comments also need to be on the next line after the command.
echo hi
::cls clears the screen
::The go to command

goto end
::skips over the echo Devan command below.
echo Devan

:end
pause